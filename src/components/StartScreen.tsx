import {useRef} from 'react';
export function StartScreen({onFile}:{onFile:(f:File)=>void}){const ref=useRef<HTMLInputElement>(null);return <main className="start"><div className="sprout" aria-hidden="true"><i/><i/><i/></div><h1>Buis<span>Son</span></h1><p className="baseline">Entre dans le BuisSon.</p><p className="story">Dans le BuisSon, cueille 5 baies qui résonnent.</p><button className="choose" onClick={()=>ref.current?.click()}><b>＋</b><span>CHOISIR UN PAYSAGE SONORE<small>MP3 · WAV · M4A</small></span></button><input ref={ref} hidden type="file" accept="audio/mpeg,audio/wav,audio/mp4,.mp3,.wav,.m4a" onChange={e=>e.target.files?.[0]&&onFile(e.target.files[0])}/><p className="privacy">Ton paysage reste ici.</p></main>}
