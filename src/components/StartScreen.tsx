import {useRef} from 'react';
export function StartScreen({onFile,restored}:{onFile:(f:File)=>void;restored?:string}){const ref=useRef<HTMLInputElement>(null);return <main className="start"><div className="brand-wave"><i/><i/><i/><i/><i/></div><p className="eyebrow">TON ÉCOUTE DEVIENT UNE CRÉATION</p><h1>Audio<span>Mood</span></h1><p className="baseline">Écoute. Ressens.<br/>Fais danser les sons.</p>{restored&&<p className="restore">Recharge ton paysage sonore<br/><strong>{restored}</strong></p>}<button className="choose" onClick={()=>ref.current?.click()}><b>＋</b><span>CHOISIR UN PAYSAGE SONORE<small>MP3, WAV ou M4A</small></span></button><input ref={ref} hidden type="file" accept="audio/*,.m4a" onChange={e=>e.target.files?.[0]&&onFile(e.target.files[0])}/><p className="privacy">Tout reste sur cet appareil.</p></main>}
