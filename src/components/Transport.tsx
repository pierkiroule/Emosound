const format=(v:number)=>`${Math.floor(v/60)}:${Math.floor(v%60).toString().padStart(2,'0')}`;
export function Transport({playing,time,duration,onToggle,onSeek,onReplay,onUndo,canUndo}:{playing:boolean;time:number;duration:number;onToggle:()=>void;onSeek:(n:number)=>void;onReplay:()=>void;onUndo:()=>void;canUndo:boolean}) {return <div className="transport"><button className="play" onClick={onToggle} aria-label={playing?'Pause':'Lecture'}>{playing?'Ⅱ':'▶'}</button><span>{format(time)}</span><input aria-label="Position de lecture" type="range" min="0" max={duration||1} step=".01" value={time} onChange={e=>onSeek(+e.target.value)}/><span>{format(duration)}</span><button className="undo" disabled={!canUndo} onClick={onUndo} aria-label="Annuler">↶</button><button className="replay" onClick={onReplay}>▶ REVOIR MON ÉCOUTE</button></div>}
