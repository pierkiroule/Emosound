import { MOODS,type MoodId,type MoodMarker } from '../types/audiomood';
export function MarkerEditor({marker,onChange,onDelete,onClose}:{marker:MoodMarker;onChange:(m:MoodId)=>void;onDelete:()=>void;onClose:()=>void}){return <div className="editor" role="dialog" aria-label="Modifier le mood"><button className="close" onClick={onClose}>×</button><p>CHANGER LE MOOD</p><div>{MOODS.map(m=><button className={m.id===marker.mood?'active':''} onClick={()=>onChange(m.id)} key={m.id}><span>{m.emoji}</span>{m.label}</button>)}</div><button className="delete" onClick={onDelete}>SUPPRIMER</button></div>}
