import { FEELINGS, type FeelingMarker } from '../types/emosound';
export function MarkerEditor({marker,onChange,onDelete,onClose}:{marker:FeelingMarker;onChange:(f:typeof marker.feeling)=>void;onDelete:()=>void;onClose:()=>void}) {return <div className="editor" role="dialog" aria-label="Modifier le ressenti"><button className="close" onClick={onClose}>×</button><p>CHANGER</p><div>{FEELINGS.map(f=><button className={f.id===marker.feeling?'active':''} onClick={()=>onChange(f.id)} key={f.id}><span>{f.emoji}</span>{f.label}</button>)}</div><button className="delete" onClick={onDelete}>SUPPRIMER</button></div>}
